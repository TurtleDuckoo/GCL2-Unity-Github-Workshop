using UnityEngine;

public class yessing : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("plsing yessing broing");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
